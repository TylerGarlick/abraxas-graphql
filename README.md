# abraxas-graphql
The GraphQL interface for Abraxas. 

## Setup
1. Copy `.env.example` to `.env`
2. Fill in the ArangoDB connection details in `.env`
3. Run `bun install`

## Testing
Run `bun test` to verify the connection to ArangoDB.
